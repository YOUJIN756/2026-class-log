// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int num;
    printf("숫자를 입력하세요:");
    scanf("%d",&num);
    
    
    
    if(num > 10)
    //if 뒤에는 ; 안 붙임. 물음을 했는데 아직 답이 나오지 않았기 때문
    // ;은 끝났을 때 붙임
    
    printf("10보다 크다");
    //10 크다 -> 10초과 출력
    // >크다 <작다 >=이상 <=이하
    
    if(num > 0)
    printf("\n양수");
    
    else
    printf("\n음수");
    
    if(num % 2 == 0)
    {printf("\n짝수");
    printf("\n2로 나눈 나머지가 0"); }
    else
    {printf("\n홀수");
    printf("\n2로 나눈 나머지가 1"); }




    return 0;
}